---
interface Props {
  href?: string,
  class?: string,
  wrap?: boolean,
}
const { href, class: className, wrap } = Astro.props
---
{
  wrap ? <a href={href} class={className}><slot/></a> : <slot/>
}